
const products_schema='id,name,description,price,isAvailable';//define el esquema de la tabla local productos

export const dbSchema={
  products: products_schema //se crea la tabla productos con el esquema definido
}
